[project]
name = "fastapi-journey"
version = "0.1.0"
description = "FastAPI learning journey"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "email-validator",
    "uvicorn[standard]",
    "pydantic",
]